{
  "Description": "Dispatch characterization: a disabled CustomDateTable entry with a ReferenceTable removes both the date table and its reference table (Table/Template are still required)",
  "Templates": [
    { "Class": "CustomDateTable", "Table": "Date", "ReferenceTable": "DateReference", "Template": "DateTemplate-01.json", "IsEnabled": false }
  ]
}
